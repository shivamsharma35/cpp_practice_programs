#include<iostream>
#include<conio.h>
using namespace std;
int main()
{

    int a,b;
    cout<< "Enter two numbers";
    cin >> a >> b;
    if(a==b)
    cout << a << "both are equal" << b ;
    else if(a>b)
    cout << a << "is greater than " << b ;
    else 
    cout << b << "is greater than " << a;
    getch();

    return 0;











}